# Solutions - Module 2

- Utiliser `cross_val_score(model, X, y, cv=5)` pour la validation.
- Pour la comparaison, entraîner chaque modèle dans un pipeline avec scaling.
- Générer les métriques via `classification_report` et `confusion_matrix`.
