# Solutions - Module 3

- MLP: Flatten -> Dense(128, relu) -> Dense(10, softmax).
- Dropout réduit l'overfitting sur validation.
- CNN attendu: Conv2D + MaxPooling + Dense, avec meilleure accuracy sur MNIST.
- LSTM: fenêtre glissante + `tf.keras.layers.LSTM`.
